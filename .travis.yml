language: python

python:
    - 2.7
    - 3.4
matrix:
    include:
        - python: 3.4
          env: CC=clang CXX=clang++

before_install:
    - uname -a
    - sudo apt-get update -qq
    - sudo apt-get install -qq libatlas-dev libatlas-base-dev liblapack-dev gfortran
    - wget http://repo.continuum.io/miniconda/Miniconda-latest-Linux-x86_64.sh -O miniconda.sh
    - chmod +x miniconda.sh
    - ./miniconda.sh -b
    - export PATH=/home/travis/miniconda/bin:$PATH
    - conda update --yes conda

# command to install dependencies
install:
    - conda install --yes python=$TRAVIS_PYTHON_VERSION pip numpy scipy nose cython
    - pip install coverage coveralls
    - "python setup.py install --with-f90mc"

# command to run tests
script: 
    - nosetests --with-coverage --cover-package=qutip qutip

after_success:
    - if [[ $TRAVIS_PYTHON_VERSION == '3.4' ]] && [[ $CC == 'gcc' ]]; then
          coveralls;
      fi

 #2017-08-27 18:57:55 modified at 0 #2017-08-27 18:58:00 modified at 1 #2017-08-27 18:58:04 modified at 2 #2017-08-27 18:58:09 modified at 3 #2017-08-27 18:58:13 modified at 4